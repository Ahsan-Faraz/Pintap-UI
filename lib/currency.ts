/** Platform default — all fallbacks and new records use EUR unless a store sets otherwise. */
export const DEFAULT_CURRENCY: string = "EUR";

/** Minimum available balance (minor units) before a recommender can request a payout. */
export const MIN_PAYOUT_MINOR = 2500;
